# Copy to .env.prod and fill in values (production only)

# PostgreSQL password — used by compose.prod.yml for both the db container and the app
POSTGRES_PASSWORD=change_me

# setlist.fm API — get a key at https://www.setlist.fm/settings/api
SETLISTFM_API_KEY=
